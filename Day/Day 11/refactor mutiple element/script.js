function createBotton (className){
    const button = document.querySelector('class');
    button.className;
}

function createNew(){
    const button = createNew();
}

function createBotton(className){
    const button = createBotton();
    return button;
}

function createIcon (){
    const icon = createIcon();
}

